# Node.js / npm / pnpm / yarn
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.pnpm-debug.log

# Next.js build artifacts
.next/
out/
build/

# Firebase CLI logs and local configs (critical to ignore these!)
firebase-debug.log
firebase-debug.*.log
.firebaserc
firebase.json   # Optional: ignore if you don't want project-specific config in git
.google-config.json

# Environment variables / secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Editor / OS files
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs and temp files
*.log
logs/
tmp/
temp/

# Testing / coverage
coverage/
.nyc_output/

# Optional: ignore large media/files if you add them later
*.jpg
*.png
*.gif
# (uncomment above if you don't want photos committed; otherwise keep small ones)

# Claude Code / temp project files (if any)
CLAUDE.md
claude-session-*

# Keep this at the end
.DS_Store